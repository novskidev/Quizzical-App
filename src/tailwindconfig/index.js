const colorConfig = {
  baby: "#DEEBF8",
  lemony: "#FFFAD1",
  primary: "#F5F7FB",
  primary2: "#293264",
  secondary: "#4D5B9E",
};

const fontConfig = {
  karla: ["Karla", "sans-serif"],
};

const config = {
  colorConfig,
  fontConfig,
};

export default config;
